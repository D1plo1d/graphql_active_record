require 'spec_helper'

# it "raises an error if the the global identifier returns the wrong type" do
#   resolver = build_resolver "NotSoLegitModelName"

#   expect{resolver.resolve_singular_field {}, {}, {}}.to raise_error
# end
